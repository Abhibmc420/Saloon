{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: App",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/app.py",
            "console": "integratedTerminal",
            "justMyCode": true,
            "env": {
                "DATABASE_URL": "postgresql://postgres:Admin123@localhost:5432/Saloon"
            }
        }
    ]
}